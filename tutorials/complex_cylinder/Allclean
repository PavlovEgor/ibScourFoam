#!/bin/sh
cd "${0%/*}" || exit                                # Run from this directory
. ${WM_PROJECT_DIR:?}/bin/tools/CleanFunctions      # Tutorial clean functions
#------------------------------------------------------------------------------

cleanCase

# Immersed-boundary solver outputs: object force data and the dual mesh
# regenerated by the solver at start-up
rm -rf pressure
rm -rf constant/plane_dual

#------------------------------------------------------------------------------
